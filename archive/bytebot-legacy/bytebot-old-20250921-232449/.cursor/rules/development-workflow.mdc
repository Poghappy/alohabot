---
description: 开发工作流程和最佳实践指南
---

# 开发工作流程

## 环境设置

### Node.js 版本管理
- **必需版本**: Node.js 20.19.4 (LTS)
- **版本文件**: [.nvmrc](mdc:.nvmrc) 确保团队使用统一版本
- **切换版本**: `nvm use` 自动读取 .nvmrc 文件

### 开发环境启动
```bash
# 1. 确保使用正确的 Node.js 版本
nvm use

# 2. 安装依赖
npm install

# 3. 配置环境变量
cp docker/.env.example docker/.env
# 编辑 docker/.env 添加必要的 API 密钥

# 4. 启动开发环境
docker-compose -f docker/docker-compose.development.yml up -d
```

## 代码组织规范

### 新功能开发流程

1. **核心模块扩展**
   - 新功能优先在 `src/core/` 下创建
   - 参考现有模块结构: [src/core/prompts/prompt-manager.ts](mdc:src/core/prompts/prompt-manager.ts)
   - 在 [src/core/index.ts](mdc:src/core/index.ts) 中导出新功能

2. **配置文件添加**
   - 配置文件放在 `config/` 对应子目录
   - 提供 JSON Schema 验证
   - 创建示例配置文件

3. **类型定义**
   - 所有公共接口都要有 TypeScript 类型
   - 导出类型定义供其他模块使用
   - 使用 JSDoc 注释说明复杂类型

### 模块集成模式

```typescript
// 1. 创建新的管理器类
export class NewFeatureManager {
    private config: NewFeatureConfig | null = null;
    
    constructor(configPath?: string) {
        // 初始化逻辑
    }
    
    public async initialize(): Promise<void> {
        // 初始化实现
    }
}

// 2. 导出单例实例
export const newFeatureManager = new NewFeatureManager();

// 3. 在核心模块中集成
// src/core/index.ts
export { NewFeatureManager, newFeatureManager } from './new-feature/manager';

// 4. 在初始化函数中添加
export async function initializeCoreModules(): Promise<void> {
    await newFeatureManager.initialize();
    console.log('✅ 新功能管理器初始化完成');
}
```

## 测试策略

### 单元测试
- 为所有核心管理器类编写测试
- 使用 Jest 作为测试框架
- 测试覆盖率目标: 80%+

```typescript
// tests/core/prompts/prompt-manager.test.ts
describe('PromptManager', () => {
    let manager: PromptManager;
    
    beforeEach(() => {
        manager = new PromptManager('test-config.yaml');
    });
    
    test('should load config successfully', async () => {
        await expect(manager.loadConfig()).resolves.not.toThrow();
    });
});
```

### 集成测试
- 测试核心模块之间的交互
- 验证配置文件加载和验证
- 测试 MCP 客户端连接

### Docker 环境测试
```bash
# 构建和测试完整环境
docker-compose -f docker/docker-compose.yml build
docker-compose -f docker/docker-compose.yml up -d

# 验证服务状态
curl http://localhost:9992/health
curl http://localhost:9991/health
```

## 代码审查标准

### Pull Request 检查清单

- [ ] 代码遵循 TypeScript 编码标准
- [ ] 所有公共方法都有 JSDoc 注释
- [ ] 新增配置有对应的 schema 验证
- [ ] 更新了相关文档
- [ ] 通过了所有测试
- [ ] Docker 构建成功

### 代码质量工具

```bash
# 代码格式化
npm run format

# 类型检查
npm run type-check

# Linting
npm run lint

# 测试
npm run test
```

## 部署流程

### 本地开发部署
```bash
# 开发环境
docker-compose -f docker/docker-compose.development.yml up -d

# 生产环境测试
docker-compose -f docker/docker-compose.yml up -d
```

### 配置管理
- 开发环境使用 `docker/.env.development`
- 生产环境使用 `docker/.env.production`
- 不同环境的配置文件分别管理

### 服务健康检查
```bash
# 检查所有服务状态
docker-compose ps

# 查看服务日志
docker-compose logs bytebot-ui
docker-compose logs bytebot-agent

# 验证 API 端点
curl http://localhost:9992
curl http://localhost:9991/api/health
```

## 文档维护

### 文档更新规范
- 新功能必须更新 [STRUCTURE_OVERVIEW.md](mdc:STRUCTURE_OVERVIEW.md)
- 重大变更需要更新 [docs/project/NEW_PROJECT_STRUCTURE.md](mdc:docs/project/NEW_PROJECT_STRUCTURE.md)
- API 变更要更新对应的 API 文档

### README 维护
- [README.md](mdc:README.md) 保持最新的功能介绍
- 包含快速开始指南
- 链接到详细文档

## 性能监控

### 关键指标监控
- 核心模块初始化时间
- MCP 工具调用响应时间
- 内存使用情况
- 对话历史存储性能

### 日志记录
```typescript
// 性能日志记录
const startTime = Date.now();
await someOperation();
const duration = Date.now() - startTime;
console.log(`🔧 操作完成，耗时: ${duration}ms`);
```

## 故障排除

### 常见问题诊断

1. **服务启动失败**
   - 检查端口占用: `lsof -i :9992`
   - 验证环境变量: `docker-compose config`
   - 查看详细日志: `docker-compose logs --tail=50 service-name`

2. **核心模块初始化失败**
   - 检查配置文件路径和格式
   - 验证 YAML/JSON 语法
   - 确认必需的环境变量已设置

3. **MCP 连接问题**
   - 检查服务器注册表配置
   - 验证网络连接
   - 查看 MCP 客户端日志

### 调试工具
```bash
# 进入容器调试
docker exec -it bytebot-agent /bin/sh

# 查看实时日志
docker-compose logs -f bytebot-ui

# 检查网络连接
docker network ls
docker network inspect bytebot_default
```

## 版本发布

### 版本号规范
- 使用语义化版本: `MAJOR.MINOR.PATCH`
- 主要版本: 不兼容的 API 变更
- 次要版本: 向后兼容的功能性新增
- 修订版本: 向后兼容的问题修正

### 发布检查清单
- [ ] 更新版本号
- [ ] 更新 CHANGELOG
- [ ] 完整测试所有功能
- [ ] 更新文档
- [ ] 构建 Docker 镜像
- [ ] 标记 Git 版本